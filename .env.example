# Required — set both for full dual-provider support
GROQ_API_KEY=your_groq_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional — override the listening port (default: 10000)
# Render injects PORT automatically; you do not need to set this on Render.
PORT=10000
